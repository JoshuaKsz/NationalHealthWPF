<UserControl x:Class="WpfAppNET.MVVM.View.Class1View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:WpfAppNET.MVVM.View"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Background="#1B1B1B">
    <Grid>

        <TextBox Name="txtFirst" Width="200" Height="25" Margin="56,40,544,385" VerticalAlignment="Top" HorizontalAlignment="Left" />

        <TextBox Name="txtSecond" Width="200" Height="25" Margin="56,79,544,346" VerticalAlignment="Top" HorizontalAlignment="Left"/>

        <Button Content="Update" Height="23" HorizontalAlignment="Left" Margin="310,40,0,0" Name="btnUpdate" VerticalAlignment="Top" Width="141" Command="{Binding Path=UpdateCommand}" />

        <Button Content="button Test" Height="23" HorizontalAlignment="Left" Margin="310,79,0,0" Name="btnTest" VerticalAlignment="Top" Width="141" Command="{Binding Path=TestCommand}" Click="btnTest_Click" />
        <ListView Name="UserGrid" Grid.Row="1" Margin="4,178,12,13" ItemsSource="{Binding PenyakitList}" Width="300">
            <ListView.View>
                <GridView x:Name="grdTest">
                    <GridViewColumn Header="id_penyakit" DisplayMemberBinding="{Binding id_penyakit}" Width="100"/>
                    <GridViewColumn Header="mikroorganisme" DisplayMemberBinding="{Binding mikroorganisme}" Width="100"/>
                    <GridViewColumn Header="nama_family" DisplayMemberBinding="{Binding nama_family}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>

</UserControl>
